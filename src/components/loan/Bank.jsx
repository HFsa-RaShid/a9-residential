import React from 'react';

const Bank = ({bank}) => {
    const {id,image,name,maximum_loan_amount,interest_rate,tenure} = bank;
    return (
        <div>
            
        </div>
    );
};

export default Bank;